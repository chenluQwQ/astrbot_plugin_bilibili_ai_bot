# BiliBot 内部架构与渐进迁移

本文描述 1.4 系列开始采用的内部边界。目标是保持现有功能和配置可用，逐步
减少评论、私信、直播、主动行为各自维护重复逻辑的情况，而不是一次性重写插件。

## 四个边界

1. **平台能力**：只负责读取和调用 B站接口，返回明确的成功、失败或不确定结果。
2. **事件运行时**：接收统一事件，维护领取、忽略、发送、成功和失败状态，并保护重复动作。
3. **角色决策**：读取允许的人格、画像与记忆，决定是否回应以及生成什么内容，不直接持有 Cookie。
4. **数据服务**：通过固定接口读写记忆、画像、日志和状态；业务模块不应自行拼接新的存储格式。

未来 WebUI 只读取这些稳定接口，不直接扫描或修改各模块内部变量。

## 统一事件

`core/runtime.py` 中的 `InboundEvent` 是评论、私信和直播弹幕的公共最小格式：

- `source`：评论、私信、直播等来源；
- `event_id`：平台事件唯一标识；
- `actor_id` / `actor_name`：当前用户；
- `conversation_id`：该事件所属对话或直播场次；
- `target_id`：视频、评论或直播间等目标；
- `priority`：跨入口通用的事件优先级；
- `metadata`：不会改变公共结构的来源补充字段。

事件使用 `platform:source:event_id` 作为进程内唯一键。已有的私信游标、评论回复
记录和直播首次定位仍负责跨重启保护，运行时不替代这些持久化机制。

## 事件优先级

同一个轮询批次按以下顺序选择事件：管理员、直接提及、活跃对话、角色感兴趣、
普通事件、后台事件。同一优先级默认按时间顺序处理；直播弹幕合并为了保证语境
新鲜度，会在同级中优先最新一条。优先级只决定处理顺序，不会绕过黑名单、
安全检查、回复概率、冷却或平台限流。

运行时状态只公开来源、状态、优先级、忽略原因和更新时间，不公开消息正文、昵称
或 UID。管理员状态页和未来 WebUI 可以使用该快照解释“为什么先处理这条”，但不
需要读取平台原始消息。

## 平台动作规则

任何会改变外部状态的行为都应创建 `ActionRequest`，包括回复、拉黑、分享、点赞、
投币、收藏、关注和动态发布。动作遵循以下规则：

- 相同动作键正在执行时，不并发再次调用平台；
- 相同动作已经成功时，不重复发送；
- 失败动作可以由明确的恢复策略重试；
- 只有平台确认成功后，才提交好感度、画像、长期记忆和“任务已完成”等副作用；
- 超时且无法确认发送结果时，后续应进入“发送不确定”，不能直接当成未发送重试。

从 v1.4.4 起，评论、私信、直播、主动看片/看番互动、LLM 写工具和动态发布共享同一个优先动作队列。
动作先以 `queued` 写入 SQLite，开始调用平台时转为 `running`，最终进入
`succeeded`、`failed` 或 `unknown`。明确失败会退还预留额度；超时、发送中取消或
重启时遗留的 `running` 动作会标记为 `unknown`，既不退还额度也不自动重发。

统一行为预算使用 SQLite 原子计数器，同时施加全局分钟/日额度以及评论、私信、
直播、主动看片和动态各自的既有硬上限。这样多个轮询器并发抢最后一个名额时只会
有一个成功，不能再通过分别读取 JSON 日志绕过总量限制。安全拉黑和 `new` 控制
确认属于明确豁免动作，但仍经过队列和幂等保护。

## 迁移顺序

1. 评论、私信、直播统一事件与回复动作（v1.4.0 已完成）；
2. 抽出全局行为预算、按来源限流和忽略原因（v1.4.4 已完成底座）；
3. 主动看片、分享、互动和动态发布接入动作执行层（v1.4.4 已完成主要写入口）；
4. 统一记忆与画像数据服务，并补充删除、导出和容量治理；
5. 最后接入 WebUI 总览、审核和管理界面。

每一步都必须保持旧配置可用，并用测试覆盖重复事件、并发发送、失败重试和副作用
提交顺序。

## 统一记忆主库（v1.4.3）

语义记忆现在以 SQLite `memories` / `memory_vectors` 为权威来源。首次升级会把
`memory.json` 按稳定 `rpid` 幂等导入；此后 JSON 文件只作为旧版本兼容备份和
SQLite 故障时的恢复日志。正常写入顺序为 SQLite → JSON，批量压缩与日终清算
持有统一写锁并原子替换快照，避免实时回复与清算互相覆盖。

评论、私信和直播在召回时必须携带 reader scope。用户对话、压缩总结、画像事实、
印象、标签和视频引用都记录来源 scope；个人内容还必须匹配当前 UID。公开视频理解
可归入 `proactive` 供各B站场景读取，但私信和直播正文不能进入公开评论上下文。

外部平台消息无权写入 `self` 永久记忆。模型从评论、私信或弹幕提取出的信息只能
写回当前来源域；`self` 仅保留管理员命令和受信后台维护的角色自我认知。
