# BiliBot 后续计划

这份文件只记录已经讨论过的方向与验收标准，不代表对应功能已经上线。实际开发时仍按安全性、B站接口可用性和测试结果调整。

## 第一优先级：记忆系统重构

先稳定记忆的含义、归属、检索和删除，再制作 Web 管理界面。不能让漂亮的管理页直接建立在含义不清的数据文件之上。

v1.4.3 已完成第一批底座：SQLite 成为语义记忆主库，旧 JSON 自动迁移并降级为恢复备份；评论、私信、直播的记忆与画像按 scope 隔离；压缩按用户和来源分开；平台发送失败不再提交画像与记忆。以下保留项继续作为后续验收清单，重点转向精确删除、向量治理与 Web 管理。

- 盘点当前对话记忆、用户画像、永久记忆、视频记忆、UP 主关系、直播记忆、日/周记录及跨插件记忆，明确每类数据的唯一职责和权威来源。
- 为记忆建立稳定 ID、来源平台、会话范围、所属用户/UP/视频、创建时间、重要度和生命周期字段，避免依赖文本前缀猜类型。
- 分清“发生过的具体事件”“对某位用户形成的画像事实”和“可长期保留的角色记忆”，禁止同一段内容在多个文件里无边界复制。
- 统一写入、检索、压缩、晋升和过期流程；评论、私信、直播与 QQ 侧使用同一套语义，但继续遵守平台可见范围。
- 检查向量检索的去重、阈值、错召回和过期索引；原文变化或删除后必须同步更新向量索引，Embedding 不可用时安全降级。
- 定义单条删除、按用户删除、按视频删除、清空画像和彻底遗忘的不同语义，并处理关联索引、摘要、计数和缓存，避免“看似删了但还能召回”。
- 设计可回滚的数据迁移：升级前备份、迁移后校验、失败恢复旧数据；不得要求用户手工删除整个 `data` 目录。
- 增加回归测试，覆盖跨平台隔离、用户不串台、视频归属、压缩后可召回、删除后不可召回、并发写入和旧数据迁移。

## 第二阶段：Web 记忆管理

- 沿用当前控制中心的视觉语言，增加记忆搜索、类型筛选、用户筛选和时间筛选。
- 详情页明确展示记忆来源、层级、关联用户/视频及是否含向量，不直接暴露向量正文。
- 支持单条删除与批量清理；删除前展示影响范围，危险操作二次确认，并尽可能提供短时撤销。
- 区分对话记忆、用户画像、视频记忆、直播记忆和永久记忆，避免把“删除画像”误解成“删除全部历史”。
- 所有写操作经过后端权限校验与审计，WebUI 不直接操作数据文件。

## 第三阶段：安全与稳定性二期（群友建议）

先盘点 PR #16 已有的隔离、授权、脱敏、审计和事件保护，再补缺口。这一阶段优先把已有安全能力做得可观察、可测试、可恢复，不重复堆叠含义相近的开关。

- 增加一键只读自检：分别检测 Cookie、B站接口、LLM、Embedding、联网搜索、外部命令、数据文件和工具适配状态，并给出明确故障位置。
- 增加缓存诊断与管理：展示缓存类型、数量、占用、更新时间、命中情况和来源版本；支持按类型刷新或清理，不在页面暴露凭据及完整敏感内容。
- 增加B站端安全工具调用审计：展示调用来源、目标工具、允许/拒绝结果和原因，继续与 QQ/AstrBot 工具权限隔离。
- 增加提示注入防护测试页：使用固定无害样例验证外部评论、私信、标题和搜索结果不会改变系统指令；规则调整必须带回归测试。
- 完善工具熔断：按 LLM、Embedding、搜索、B站接口和单个工具分别统计失败；支持冷却、半开恢复、手动复位与清晰状态提示，避免一个接口故障拖停全部功能。
- 整理群友意见，把重复项合并成测试用例或验收标准，再决定是否需要新增机制。

## 后续阶段：直播间管理

- 增加不良弹幕识别，优先采用“明确规则 + 模型复核”，避免只凭情绪模型直接处罚。
- 支持观察模式：只记录建议处理结果，不执行禁言或移出，便于先调规则。
- 实际管理操作必须校验 Bot 是否拥有房管权限，并提供总开关、白名单、冷却、单场上限和审计记录。
- 区分广告引流、辱骂攻击、色情内容、刷屏和普通玩笑；证据不足时不处罚。
- 调研B站当前可用的禁言、移出及解除操作接口；接口不稳定时必须安全降级，不能反复重试。

### 研究项：听懂主播后参与互动

- 评估直播音频获取、语音识别、延迟、成本与授权边界。
- 如果可行，先做“只生成上下文、不主动抢话”的实验模式，再决定是否参与弹幕互动。
- 明确区分主播语音、背景音乐和连麦观众，无法可靠区分时不把识别结果写成事实记忆。

## 再后续：新手引导与动效

- 首次打开控制中心时，用可跳过的短引导介绍账号连接、人设、功能开关、安全工具和运行状态。
- 参考本地提供的 `coverflow` HTML 动效思路制作步骤切换，但按现有 WebUI 重新实现，不直接复制外部页面。
- 支持“稍后再看”和从帮助入口重新播放；记住完成状态，不在每次启动时打扰。
- 遵循 `prefers-reduced-motion`，低性能设备和减少动态效果设置下自动使用简化过渡。
- 动画只负责解释，不遮挡配置保存、错误提示或关键安全信息。
